# Project Website

This directory is the static project website for HD-MCRA safe quadruped navigation. It includes the AGIROS demonstration video, method figure, and experiment sequences used by the page.

The GitHub Actions workflow at `.github/workflows/deploy-webui.yml` deploys this directory to GitHub Pages whenever `webui/` changes on `master`.
